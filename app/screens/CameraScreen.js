import React from "react";
import { Text, View } from "react-native";

function CameraScreen(props) {
  return (
    <View
      style={{
        backgroundColor: "white",
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Camera Screen Will open</Text>
    </View>
  );
}

export default CameraScreen;
